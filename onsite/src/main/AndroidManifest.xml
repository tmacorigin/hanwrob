<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tmac.onsite">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppBaseTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GuideActivity"></activity>
        <activity android:name=".activity.ActivationActivity"></activity>
        <activity android:name=".activity.IdentifyActivity"></activity>

        <!--
        <activity
            android:name=".activity.MainActivity">
        </activity>
        -->

        <activity
            android:name=".activity.DetailTaskActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".activity.DetailNoBeginActivity"></activity>
        <activity android:name=".activity.UploadImgActivity"></activity>
        <activity android:name=".activity.SelectImgActivity"></activity>
        <activity android:name=".activity.LoginActivity"></activity>
        <activity android:name=".activity.CannotFinishActivity"></activity>
        <activity android:name=".activity.DisplayConstructImgActivity"
            android:theme="@style/customTheme"></activity>
        <activity
            android:name="me.tmac.photopicker.PhotoPickerActivity"
            android:theme="@style/customTheme" />
        <activity
            android:name="me.tmac.photopicker.PhotoPagerActivity"
            android:theme="@style/customTheme" />
        <activity
            android:name="me.tmac.photopicker.PhotoCamDisplayActivity"
            android:theme="@style/customTheme" />
    </application>

</manifest>